{
  "name": "insurance-platform",
  "private": true,
  "version": "0.0.0",
  "description": "보험 플랫폼 프론트 모노레포 (web + mobile WebView 브릿지)",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter @insurance/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
