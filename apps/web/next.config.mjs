/** @type {import('next').NextConfig} */
const nextConfig = {
  // 워크스페이스 패키지(@insurance/*)를 web에서 쓰게 되면 여기에 추가:
  // transpilePackages: ["@insurance/bridge", "@insurance/shared"]
};

export default nextConfig;
