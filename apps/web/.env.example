# Sentry — DSN 미설정 시 SDK 완전 no-op (로컬 dev·CI E2E의 기본 상태)
NEXT_PUBLIC_SENTRY_DSN=
# production(기본) | dev — 배포 환경 구분 태그
NEXT_PUBLIC_SENTRY_ENVIRONMENT=

# 이하 3개는 소스맵 업로드(빌드) 전용 — CI에서만 주입, 로컬 개발엔 불필요
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
