# WebView가 로드할 웹 URL. 미설정 시 dev 기본값(Android 에뮬레이터 10.0.2.2:3000, 그 외 localhost:3000).
# 실기기(Expo Go)에서 dev 서버 접속 시 PC의 LAN IP로 지정: http://192.168.x.x:3000
# prod 배포 URL 확정 시 여기에 지정 (후속 이슈)
EXPO_PUBLIC_WEB_URL=
