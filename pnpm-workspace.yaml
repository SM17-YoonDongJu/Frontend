packages:
  - "apps/*"
  - "packages/*"

# 빌드 스크립트 실행을 허용할 의존성 (pnpm 11: onlyBuiltDependencies → allowBuilds 맵으로 대체)
allowBuilds:
  sharp: true
  unrs-resolver: true
  msw: true
  esbuild: true
  '@sentry/cli': true
